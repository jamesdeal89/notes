# prints the name of the numeric numbers inputed
finalAnswer = []

numbers = ["zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"]

def printALL():
    for x in finalAnswer:
        print(x)

def begin():
    lengthNum = len(str(numeric))
    if `



while True:
    numeric = int(input("enter a numeric number e.g 768:"))
    if numeric < 0 or numeric > 1000000:
        print("that is not a valid range (1-1,000,000 only)")
    else:
        begin()
    
